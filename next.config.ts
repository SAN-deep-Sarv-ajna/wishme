import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable the development indicator badge (the Next.js mark at bottom of screen)
  devIndicators: false,
};

export default nextConfig;
